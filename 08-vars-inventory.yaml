- name: varible from files
  hosts: localhost
  vars_files:
  tasks:
  - name: print information 
    ansible.builtin.debug:
      msg: "H, we are learning {{course}} from Trainer{{Trainer}}, Duration is {{Duration}}."