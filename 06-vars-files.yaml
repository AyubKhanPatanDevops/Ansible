- name: varibles files
  hosts: localhost
  vars_files:
  - vars.yaml
  tasks:
  - name: print override information
    ansible.builtin.debug:
      msg: "Hi we are learningv{{course}} from trainer{{Trainer}}, Duration is {{Duration}}."